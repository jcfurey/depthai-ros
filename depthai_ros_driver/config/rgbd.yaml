/**:
  ros__parameters:
    pipeline_gen:
      i_nn_type: none
      i_enable_rgbd: true
      i_pipeline_type: rgbd
    stereo:
      i_depth_preset: DEFAULT
      # Optional disparity-to-depth override in pixels. Leave at default to use calibration.
      # i_focal_length: 860.0
    # AUTO transport selects device-side encoding for PoE/USB2 and raw images
    # for USB3. Override driver.i_transport_profile when subpixel depth or a
    # specific transport is required.
