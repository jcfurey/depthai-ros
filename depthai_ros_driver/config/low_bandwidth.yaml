/**:
  ros__parameters:
    driver:
      i_transport_profile: LOW_BANDWIDTH
    rgb:
      i_low_bandwidth: true
    left:
      i_low_bandwidth: true
    right:
      i_low_bandwidth: true
    # The driver keeps RVC2 stereo raw because RAW8 disparity cannot be encoded.
